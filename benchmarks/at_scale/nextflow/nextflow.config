/*
 * bwa-neo at-scale benchmarks — run via Pixi from benchmarks/at_scale (see README).
 */
params {
    outdir = 'results'
    // Explicit flag so CLI can disable baseline even if BWA_BASELINE is set in the environment
    enable_baseline = false
    bwa_neo = System.getenv('BWA_NEO') ?: "${projectDir}/../../../build-benchmark/bwa"
    bwa_baseline = System.getenv('BWA_BASELINE') ?: ''
    bwa_mem2 = ''
    ref_fa = "${projectDir}/../../../tests/fixtures/tiny/ref.fa"
    reads_fq = "${projectDir}/../../../tests/fixtures/tiny/reads.fq"
    threads_aln = 1
    threads_samse = 1
    threads_sampe = 1
    threads_mem = 4
    enable_simulation = false
    rng_seed = 11
    sim_modern_profile = 'art_illumina_short'
    sim_ancient_profile = 'gargammel_default'
}

profiles {
    standard {
        process.executor = 'local'
    }
}
