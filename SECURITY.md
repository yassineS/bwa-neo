# Security

Report sensitive issues (memory corruption, unsafe defaults) privately to repository maintainers (set `SECURITY_CONTACT` in your fork’s GitHub Security policy or `MAINTAINERS.md`).

For non-sensitive bugs, use public GitHub Issues with a minimal reproducer (small FASTA/FASTQ, exact command line, expected vs actual).
