{
  "name": "bwa-neo",
  "image": "mcr.microsoft.com/devcontainers/cpp:1-ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "sudo apt-get update -q && sudo apt-get install -q -y zlib1g-dev cmake ninja-build samtools python3 python3-pip && chmod +x scripts/install-seqera-ai.sh scripts/check-seqera-ai.sh && sudo npm install -g seqera && seqera --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "nextflow.nextflow"
      ]
    }
  },
  "remoteEnv": {
    "SEQERA_AI_CWD": "${containerWorkspaceFolder}"
  }
}
